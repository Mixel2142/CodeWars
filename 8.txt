#include <list>
#include <iostream>
#include <math.h>
#include <vector>
using namespace std;

int countZ_2_Z(const list<int> &array)
{
int count =0;
for(auto &it:array)if(it==0)count++;
return count%2 == 0 ? 1 : 0;
}


int last_digit(list<int> array) {
    vector<int> var(*array.end());
    for(auto &it:array)var.at(it)=it;
    if(array.begin()==array.end())return 1;
    for(auto &it:var)cout<<it<<" ";
    cout<<endl;
    
    int firstI=var.at(var.size()-2);
    int lastI=firstI;
     cout<<"firstI="<<firstI<<endl;
    if(firstI==0)return countZ_2_Z(array);
    if(firstI==1 )return 1;
    if(firstI%10 == 1)return 1;
    if(firstI%10 == 5)return 5;
 /*
    for(auto &it:array)
    {
    
    if(it>=10)
    {
    int powId=(it/10)%10;
    int powIn=it%10;
    firstI = pow(firstI,2*powId);
    for(int i=1;i<powIn;i++)firstI *= lastI;
    }
    else
    {
    firstI=pow(firstI,it);
    }
    firstI=firstI%10;
    lastI=firstI;
    }
    */
    
    return firstI;
}